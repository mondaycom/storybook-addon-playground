import { PlaygroundParameters } from "@/types";

export const DEFAULT_ADDON_PARAMETERS: PlaygroundParameters = {
  storyId: "playground",
  components: {},
  autocompletions: {},
  editorTheme: null,
  introCode: null,
};
